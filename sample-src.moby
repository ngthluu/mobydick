// Fibonacci
fib = (n) => {
    => 1 ? n == 1 // Special case
    => 1 ? n == 2 // Special case
    => self(n-2) + self(n-1) // Normal case
}

print("The 10th element of the Fibonacci sequence: " + fib(10))